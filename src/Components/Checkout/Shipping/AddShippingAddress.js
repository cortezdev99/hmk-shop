import React from "react";
import ShippingForm from "./ShippingForm";

const AddShippingAddress = () => {
  return (
    <div className="add-shipping-address-container">
      <div className="add-shipping-address-header">Add Shipping Address</div>
      <ShippingForm />
    </div>
  );
};

export default AddShippingAddress;
